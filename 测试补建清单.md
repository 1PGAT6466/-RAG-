# 「更新RAG框架」测试补建清单（可执行）

> 生成时间：2026-08-27
> 依据：48 个无测试的 src 业务模块，按「核心链路优先」排序
> 排序规则：被引用次数(核心度) × 代码量(复杂度) × 业务关键度

---

## 一、总体结论

- 当前测试覆盖：仅 4 个模块有测试（`classification` / `entity_extractor` / `ranking` / `tokenizer`）
- **未覆盖业务模块：48 个**，其中核心链路（pipeline / retrieval / mcp / chat / api）几乎全军覆没
- `requirements.txt` 中 `pytest` 处于注释状态（`# pytest>=7.0.0`），需先启用

### 前置准备
```bash
pip install pytest pytest-cov
# 或在 requirements.txt 取消注释：pytest>=7.0.0

# 运行方式（项目根目录）
pytest tests/ -v
pytest tests/ --cov=src --cov-report=html   # 生成覆盖率报告
```

---

## 二、补测优先级排序

### 🔴 P0 — 核心数据链路（最高优先，占项目价值 70%）

这些是被引用最多、承载核心 RAG 流程的模块，任何回归都会影响整体功能。

| 模块 | 行数 | 被引用 | 补测重点 |
|------|------|--------|---------|
| `src/pipeline/engine.py` | 347 | 6 | 任务状态机(pending→running→done/failed)、断点续跑、批量回调 |
| `src/pipeline/parser.py` | 325 | 3 | 文档解析、chunk 切分边界、异常文件处理 |
| `src/pipeline/ingest_stages.py` | 282 | 1 | 各阶段(embed/索引)进度上报、失败重试 |
| `src/pipeline/embedder.py` | 207 | 5 | 向量化、批量embed、远程/本地切换 |
| `src/pipeline/chunker.py` | 98 | 2 | 分块逻辑、边界条件、空文本 |
| `src/retrieval/search.py` | (已有) | 6 | ⚠️ 需补检索链路集成测试 |
| `src/retrieval/rerank.py` | 200 | 1 | 重排序、多策略融合、空结果降级 |
| `src/retrieval/graph_recall.py` | 148 | 1 | 图谱召回、实体关联 |
| `src/llm.py` | 216 | 7 | LLM 调用、超时、流式响应、失败重试 |

### 🟡 P1 — 交互与对外接口

| 模块 | 行数 | 被引用 | 补测重点 |
|------|------|--------|---------|
| `src/chat/router.py` | 238 | 3 | 意图路由、fallback 链 |
| `src/chat/orchestrator.py` | 199 | 1 | 多步骤编排、上下文传递 |
| `src/chat/engine.py` | 145 | 6 | 对话引擎主流程 |
| `src/chat/cache.py` | 156 | 1 | 缓存命中/失效、TTL |
| `src/mcp/translate.py` | 219 | 1 | 翻译队列、批量、错误处理 |
| `src/mcp/client.py` | 111 | 1 | MCP 客户端、超时、崩溃隔离 |
| `src/mcp/manager.py` | 95 | 1 | 插件/工具生命周期管理 |
| `src/api/documents.py` | 167 | 1 | 文档 CRUD、上传、状态流转 |

### 🟢 P2 — 插件与扩展

| 模块 | 行数 | 被引用 | 补测重点 |
|------|------|--------|---------|
| `src/plugins/host.py` | 148 | 2 | 插件宿主、异常降级 |
| `src/plugins/registry.py` | 95 | 3 | 插件注册/发现/卸载 |
| `src/plugins/hooks.py` | 71 | 2 | 钩子触发顺序、容错 |
| `src/plugins/lifecycle.py` | 64 | 1 | 生命周期状态 |
| `src/extraction/relation_builder.py` | 250 | 2 | 实体关系构建、去重 |
| `src/extraction/llm_worker.py` | 109 | 1 | worker 并发、去重集 |

### ⚪ P3 — 辅助模块

其余模块（auth/api 其余、web_search、relevance、smithery、image_extractor、ocr_engine、markdown_render、llm_audit 等）可后续逐步补充。

---

## 三、推荐的落地方式（3 步走）

**Step 1：先给 P0 的 9 个核心模块补测试（1~2 天）**
```python
# tests/test_pipeline_engine.py 示例
import pytest
from src.pipeline.engine import PipelineEngine

def test_task_lifecycle():
    engine = PipelineEngine()
    tid = engine.submit(...)
    assert engine.status(tid) == "pending"
    # 模拟完成 → 断言 done
```

**Step 2：配置覆盖率门槛（CI）**
```yaml
# 建议在 CI 中加
pytest tests/ --cov=src --cov-fail-under=30  # 先设 30%，逐步提升到 60%+
```

**Step 3：核心链路集成测试（pipeline → retrieval → chat 端到端）**
- 插入测试文档 → 跑完整 ingest → 断言 chunk 落库 → 检索断言召回 → 对话断言引用

---

## 四、关键风险提示

1. **`src/pipeline/engine.py`（347 行，被 6 处引用）** 是最大单点，最优先补测。
2. **`src/llm.py`（216 行，被 7 处引用）** 是几乎所有链路的上游，失败重试逻辑必须测。
3. 现有测试只用了 `from src.xxx import` 的绝对导入，说明项目已规范为 `src` 包结构，pytest 需在根目录运行（`pytest tests/`）才正确。

---

*本清单由项目管理台深度检测自动生成 + AI 复核排序。*
